<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" />
    <corners android:radius="50dp" /> <!-- 调整此值来控制圆角的弧度 -->
    <stroke
        android:width="2dp"
        android:color="@android:color/white" />
</shape>
